# C:\CodexApp\docker-compose.yml
services:
  codex:
    build: .
    volumes:
      - ./src:/usr/src/app/src         # Mount only src folder
    working_dir: /usr/src/app
    env_file:
      - .env                       # Load environment variables from .env
    tty: true                      # Keep the shell open for interaction

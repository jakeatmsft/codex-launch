# C:\CodexApp\docker-compose.yml
services:
  codex:
    build: .
    volumes:
      - C:\CodexApp:/usr/src/app    # Mount your Windows folder
    working_dir: /usr/src/app
    env_file:
      - .env                       # Load OPENAI_API_KEY from .env
    tty: true                      # Keep the shell open for interaction
